안드로이드 미니 퀴즈

2-1
1. 이미지와 같은 리소스를 저장하는 디렉터리의 이름? res
2. 내가 만드는 앱의 빌드 정보가 들어 있는 파일명? build.gradle
3. 애뮬레이터의 속도를 빠르게 해주는 하드웨어 가속기의 이름은? intel x86 Emulator Accelerator(HAXM)

3-1
1. 실제 에러를 출력하기 위한 로그함수의 이름? Log.e()
2. 개발자들이 보기 위한 목적으로 사용되는 로그함수의 이름? Log.d()
3. 특정 로그를 필터링하기 위해 사용되는 로그함수의 첫 번째 파라미터는? 태그

3-2
1. 코드 작성 규칙에서 가장 중요한 것은? 
들여쓰기(코드 관례를 따르고, 일정한 들여쓰기 유지 -> 가독성, 주석)
2. 16비트의 정수값을 저장할 수 있는 타입은? 
Short, Int, Long
3. 2개 문자열을 합칠 수 있는 방법은? 
문자열 템플릿 +, "${}"
4. 한 번 입력한 값을 바꿀 수 없는 변수? 
val

3-3
1. if문의 수식이 false일 때 특정한 코드를 처리하기 위해서는 어떤 문법을 사용?
if ~ else, else if

2. when문에서 범위 값을 비교하기 위해서 무엇을 사용?
in을 사용

3. when문에서 비교 대상 파라미터가 없어도 사용할 수 있습니다. O

4. 변수 result에 입력되는 값은 무엇인가요?
var result = when(10) {
	9 -> {true}
	in 5..20 -> {false}
	else -> {true}
} ->false


3-4
1. 여러 개의 값을 하나의 변수에 담을 수 있는 데이터 타입?
배열, 리스트, 셋, 맵
2. 리스트와 셋의 가장 큰 차이점은 무엇인가요? 
리스트 - 인덱스, 셋(key, value)
리스트 : 데이터에 인덱스를 부여한 컬렉션이며 중복된 값을 입력할 수 있음.
셋 : 리스트에서 인덱스가 빠진 데이터 타입 특정 위치 조회 X. 중복을 허용하지 않는 리스트
3. 배열은 뮤터블인가요? O, 값은 바꿀 수 있었지만 크기는 바꿀 수 없음
var로 선언된 변수는 모두 뮤터블
4. 다음 코드의 결과로 출력되는 값은 무엇인가요? Null값 확인
var map = nutableMapOf<String, String>()
map.put("키1", "값2")
map.put("키2", "값2")
map.put("키3", "값3")
map.remove("키3")
Log.d("Collection","${map.get("키3")}")

3-5

1. 1부터 100까지 반복하면서 숫자를 출력하는 코드에는 for와 while중 어떤 반복문을 사용하는 것은 좋나요?
for문

2. while문과 do~ while문의 차이점은?
while문은 조건을 만족해야 실행되는 반면, do~while문은 한 번 실행되고 조건을 만족하면 계속해서 실행하는 차이점이 있음.

3.
for (index in 0..1000) {
	if(index > 999) {
		Log.d("for", "$index")
	}
}
 

4. 
var result = 1
while (result < 100) {result += result}
Log.d("while", "$result")


3-6
1. 2개의 Int 타입 입력 파라미터를 가지고 입력된 2개의 값을 더한 후에 반환하는 함수


3-7 
1. 클래스의 멤버 변수와 멤버 함수를 지칭하는 용어는 무엇인가?
프로퍼티와 메서드
2. 클래스를 사용하기 위해 호출되는 함수?
생성자
3. 생성자를 통하지 않고 클래스의 멤버를 사용할 수 있게 해주는 키워드
companion object
4. 상속 관계에서 자식이 부모의 멤버에 접근하는 것을 제한하는 것
접근 제한자
5. 접근 제한자
public, internal(같은 모듈), protected, private
6. 아무런 접근 제한자도 지정하지 않으면 어떻게 동작하는지?
public
7. 추상화


3-9
1. () 안에 들어가야 하는 키워드는 무엇인가요?
lateinit(var) school: School
2. () 안에 들어가야 하는 키워드는 무엇인가요?
(val) apple: Apple by lazy {Apple()}
3. 초기화가 되어있지 않기 때문에 오류


3-10
1. 마지막 실행 코드를 반환하는 스코프 함수는?
let, run, with
2. 스코프 함수 안에서 it으로 사용되는 것 2개는?
let, also
3. count() 함수 반환

